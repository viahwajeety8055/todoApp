const loginConstants = require("./login.constants");
const registerConstants = require("./register.constants");

const userModuleConstants = {
  login: loginConstants,
  register: registerConstants,
};

module.exports = userModuleConstants;
